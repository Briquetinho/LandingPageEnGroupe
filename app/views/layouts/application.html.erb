<!DOCTYPE html>
<html>

<%= render 'layouts/head' %>

<body>
	<%= render 'layouts/header' %>

	<div class="container">
		<%= yield %>
		<%= debug(params) if Rails.env.development? %>
	</div>
	<%= render 'layouts/pop-up' %>
</body>
<%= render 'layouts/footer' %>

</html>
